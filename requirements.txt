# ASFDK Python runtime dependencies — the four Solidarity Framework pillars.
# ASFDK is the umbrella: it depends on the published pillar distributions on
# PyPI (it does not vendor them). Keep in sync with pyproject.toml [project].dependencies.
nlt-toi>=1.0.0
nlt-otoi>=1.1.0
rrt-advocate>=0.1.1
sleepwalker-protocol>=1.0.1
pytest-asyncio>=0.21
